from django.apps import AppConfig


class DevicesConfig(AppConfig):
    name         = 'apps.devices'
    verbose_name = '🖥️ Équipements & Supervision'
    default_auto_field = 'django.db.models.BigAutoField'