# ─────────────────────────────────────────────────────────────
# ElectroSecure Platform — Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs
# NE JAMAIS COMMITTER .env dans git
# ─────────────────────────────────────────────────────────────

# Django
DJANGO_SETTINGS_MODULE=config.settings.development
SECRET_KEY=changeme-generate-with-python-secrets-token-hex-32

# Base de données (production uniquement)
DATABASE_URL=postgresql://electrosecure:password@localhost:5432/electrosecure_db

# Redis (Celery + Cache)
REDIS_URL=redis://localhost:6379/0

# Hosts autorisés (production uniquement, virgule-séparés)
ALLOWED_HOSTS_LIST=api.electrosecure.com,www.electrosecure.com

# CORS (production uniquement, virgule-séparés)
CORS_ALLOWED_ORIGINS=https://electrosecure.com,https://app.electrosecure.com

# Email
DEFAULT_FROM_EMAIL=noreply@electrosecure.com
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=your-sendgrid-api-key

# OAuth2 Microsoft Azure AD (SSO)
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=https://api.electrosecure.com/api/v1/auth/sso/microsoft/callback/

# OAuth2 Google (SSO)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://api.electrosecure.com/api/v1/auth/sso/google/callback/

# Sentry (monitoring erreurs en production, optionnel)
SENTRY_DSN=
